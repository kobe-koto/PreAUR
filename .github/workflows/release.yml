name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build_and_release:
    name: Build and Release preaur
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Bun runtime
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install Dependencies
        run: bun install

      - name: Build Project
        run: |
          bun run build # build PreAUR
          bun run gen-schema # generate preaur.schema.json

      - name: Prepare Artifact
        run: |
          mv ./dist/index.js ./dist/preaur.min.js
          mv ./preaur.schema.json ./dist/preaur.schema.json

      - name: Generate Release Notes
        env:
          RELEASE_VERSION: ${{ github.ref_name }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          bash ./.github/generate-release-notes.sh

      - name: Upload Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          body_path: release.md
          files: ./dist/preaur.min.js, ./dist/preaur.schema.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
